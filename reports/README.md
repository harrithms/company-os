# What was generated

Derived from `os/` and `data/`. Safe to overwrite, because nothing originates here.

A report should never be the only place a fact lives. If you find yourself typing a number into
a report, it is missing from `data/`.

```
reports/
├── weekly/<date>.md
├── monthly/<YYYY-MM>/<team>.md
└── board/<quarter>/<section>.md
```
